namespace Cardofun.Core.NameConstants
{
    public static class SystemConstants
    {
        /// <summary>
        /// Name of a folder that keeps all the system documet templates under a root/... path
        /// </summary>
        /// <returns></returns>
        public const string DocumentTemplates = nameof(DocumentTemplates);
    }
}